
language: node_js

node_js:
  - 4.2.3

build:
  cache: true
  cache_dir_list:
    - $SHIPPABLE_BUILD_DIR/node_modules

  ci:
    - NODE_ENV=development npm install
    - npm test

branches:
  only:
    - master
