language: none

build:
  ci:
    - echo 'Building base image for retryer.js'
    - export DOCKER_IMAGE_NAME=ykrevnyi/retryerjs-base
    - ./ci/build.sh

  post_ci:
    - ./ci/push.sh

integrations:
  hub:
    - integrationName: personal_dockerhub
      type: docker
