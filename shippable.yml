language: none

build:
  ci:
    - echo 'CI Running'
    - pwd
    - ls -la
    - docker build -t test-build:test-tag -f container_images/Dockerfile-nodejs-v7.1.0 container_images/
    - docker push ykrevnyi/test-build:test-tag

integrations:
  hub:
    - integrationName: personal_dockerhub
      type: docker
